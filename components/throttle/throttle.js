// components/throttle/throttle.js
import {
  extendComponent
} from '../../common/component';
import {isObj,isArray,throttle} from '../../utils/util';

extendComponent({
  /**
   * 组件的属性列表
   */
  properties: {
    type:{
      type:Boolean,
      value:true
    }
  },

  /**
   * 组件的初始数据
   */
  data: {

  },

  /**
   * 组件的方法列表
   */
  methods: {

  }
})
