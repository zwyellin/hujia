

<navigationBar percent="{{percent}}" bgImage="{{}}" bgClass="{{bgClass}}" textColor="{{textColor}}" navBarBfc="{{navBarBfc}}" backStyle="{{backStyle}}" contentText="{{contentText}}"></navigationBar>

<image style="width:100%;" src="../../images/one/timg.jpg" mode="aspectFit|aspectFill|widthFix" lazy-load="false" binderror="" bindload="">
    
</image>

<progress  type="{{1}}" percent="{{percent}}" incrementRatio="{{increment}}" progress-class="progressTest" percentShow="{{false}}">
</progress>
<view style="margin-top:30rpx"></view>
 
<progress  type="{{2}}" percent="{{percent}}" incrementRatio="{{increment}}" progress-class="progressTest" percentShow="{{false}}">
</progress>

<view class="progressTest"></view>

<modal show="{{modalShow}}" type="{{modalType}}">
    <view style="width:400rpx;height:200rpx;background:red;">这个是{{modalType}}</view>
</modal>
<view catch:tap="modalShowTap">modal显示</view>

<notify show="{{notifyShow}}">祝福送到附近开了打手犯规</notify>


<!-- <modal show="{{true}}" type="{{'center'}}" close="{{true}}">
    <view style="width:400rpx;height:100%;background:red;">这个是bottom</view>
</modal> -->